nclude <stdio.h>
#include <stdlib.h>

/**
 *  * _islower - checks for lowercase
 *   * @i: i is an ascii char
 *    * Return: true if lowercase
 *     */

int _islower(int i)
{
		if (i >= 'a' && i <= 'z')
				{
							return (1);
								}
			else
					{
								return (0);
									}
}
