#include "main.h"
#include <stdio.h>

/**
 * print_square - no way it prints square
 * @size: size of the square
 */

void print_square(int size)
{
	int rows;
	int columns;

	if (size <= 0)
	{
		putchar('\n');
	}
	for (rows = 0; rows < size; rows++)
	{
		for (columns = 0; columns < size; columns++)
		{
			for (columns = 0; columns < size; columns++)
		}
		putchar('\n');
	}
}
